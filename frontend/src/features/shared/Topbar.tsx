import { LogOut, Menu } from "lucide-react";

export function Topbar({ title, role, userName, onMenu, onLogout }: { title: string; role: string; userName: string; onMenu: () => void; onLogout: () => void; }) {
  return <header className="sticky top-0 z-30 flex min-h-14 items-center justify-between border-b border-[#30343a] bg-[#17191c] px-3 py-2 sm:px-5"><div className="flex min-w-0 items-center gap-3"><button className="crm-icon-button lg:hidden" onClick={onMenu} aria-label="Open navigation" title="Open navigation"><Menu className="size-4" /></button><div className="min-w-0"><p className="font-mono text-[9px] uppercase tracking-[.12em] text-slate-500">{role} workspace</p><h1 className="truncate text-sm font-semibold text-slate-100">{title}</h1></div></div><div className="flex items-center gap-3"><p className="hidden text-xs text-slate-400 sm:block">{userName}</p><button onClick={onLogout} className="crm-icon-button" aria-label="Log out" title="Log out"><LogOut className="size-4" /></button></div></header>;
}
