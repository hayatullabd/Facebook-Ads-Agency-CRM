import { useMemo, useState } from "react";
import { CircleDollarSign, ReceiptText, Search } from "lucide-react";
import type { Invoice, Role } from "../../../types/crm";
import { formatDate, formatMoney } from "../../../lib/formatters";
import { Card } from "../../shared/Card"; import { StatusBadge } from "../../shared/StatusBadge";
const formatInvoiceTotals = (invoices: Invoice[], fallbackCurrency = "USD") => { const totals = new Map<string, number>(); invoices.forEach((invoice) => totals.set(invoice.currency, (totals.get(invoice.currency) || 0) + invoice.amount)); return totals.size ? [...totals.entries()].map(([currency, amount]) => formatMoney(amount, currency)).join(" · ") : formatMoney(0, fallbackCurrency); };
export function BillingPage({ invoices, role, onMarkPaid }: { invoices:Invoice[]; role:Role; onMarkPaid:(id:string)=>Promise<void> }) { const [search,setSearch]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(""); const canManage=role==="admin"||role==="team"; const filtered=useMemo(()=>invoices.filter(i=>!search.trim()||[i.invoiceNumber,i.pageName,i.objective,i.client?.name].some(v=>v?.toLowerCase().includes(search.toLowerCase()))),[invoices,search]); const mark=async(id:string)=>{setBusy(id);setError("");try{await onMarkPaid(id)}catch(err){setError(err instanceof Error?err.message:"Could not update invoice")}finally{setBusy("")}}; const totalText=formatInvoiceTotals(invoices), unpaidText=formatInvoiceTotals(invoices.filter(i=>i.status!=="Paid")); return <div className="space-y-4"><div><h2 className="text-lg font-semibold">Billing & finance</h2><p className="text-sm text-slate-500">Invoice status and outstanding balances</p></div><div className="grid gap-3 sm:grid-cols-2"><Card className="p-4"><p className="text-[11px] uppercase text-slate-500">Total billed</p><p className="mt-2 break-words text-xl font-bold leading-tight">{totalText}</p></Card><Card className="p-4"><p className="text-[11px] uppercase text-slate-500">Outstanding</p><p className="mt-2 break-words text-xl font-bold leading-tight text-red-400">{unpaidText}</p></Card></div>{error&&<div role="alert" className="rounded-md border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-400">{error}</div>}<Card><div className="border-b border-[#20293a] p-3"><div className="relative max-w-md"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-600"/><input aria-label="Search invoices" className="crm-input pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search invoices..."/></div></div>{filtered.length?<div className="crm-responsive-table overflow-x-auto"><table className="w-full"><thead className="crm-table-head"><tr><th className="px-4 py-3">Invoice</th><th className="px-4 py-3">Client</th><th className="px-4 py-3">Due date</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Status</th>{canManage&&<th className="px-4 py-3 text-right">Action</th>}</tr></thead><tbody>{filtered.map(i=><tr key={i._id} className="hover:bg-white/[0.02]"><td className="crm-table-cell"><p className="font-mono font-medium text-blue-400">{i.invoiceNumber}</p><p className="text-xs text-slate-500">{i.pageName} · {i.objective}</p></td><td className="crm-table-cell">{i.client?.name||"—"}</td><td className="crm-table-cell">{formatDate(i.dueDate)}</td><td className="crm-table-cell font-semibold text-slate-200">{formatMoney(i.amount,i.currency)}</td><td className="crm-table-cell"><StatusBadge tone={i.status==="Paid"?"success":i.status==="Overdue"?"danger":"warning"}>{i.status}</StatusBadge></td>{canManage&&<td className="crm-table-cell text-right"><button disabled={i.status==="Paid"||busy===i._id} onClick={()=>mark(i._id)} className="inline-flex items-center gap-1.5 rounded-md border border-[#263044] bg-[#0d121e] px-3 py-1.5 text-xs text-slate-300 disabled:opacity-40"><CircleDollarSign className="size-3.5"/>Mark Paid</button></td>}</tr>)}</tbody></table></div>:<div className="crm-empty"><ReceiptText className="size-5"/>No invoices found.</div>}</Card></div> }
