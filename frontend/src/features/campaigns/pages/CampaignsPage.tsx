import { useMemo, useState } from "react";
import { Megaphone, Pause, Play, Search } from "lucide-react";
import type { Campaign, Role } from "../../../types/crm";
import { formatMoney } from "../../../lib/formatters";
import { Card } from "../../shared/Card"; import { StatusBadge } from "../../shared/StatusBadge";
export function CampaignsPage({ campaigns, role, onUpdateCampaign }: { campaigns:Campaign[]; role:Role; onUpdateCampaign:(id:string,payload:Partial<Campaign>)=>Promise<void> }) { const [search,setSearch]=useState(""),[error,setError]=useState(""); const [busy,setBusy]=useState(""); const canManage=role==="admin"||role==="team"; const filtered=useMemo(()=>campaigns.filter(c=>!search.trim()||[c.name,c.objective,c.client?.name].some(v=>v?.toLowerCase().includes(search.toLowerCase()))),[campaigns,search]); const update=async(c:Campaign,status:"active"|"paused")=>{setBusy(c._id);setError("");try{await onUpdateCampaign(c._id,{status})}catch(err){setError(err instanceof Error?err.message:"Could not update campaign")}finally{setBusy("")}}; return <div className="space-y-4"><div><h2 className="text-lg font-semibold">Campaigns</h2><p className="text-sm text-slate-500">Performance and delivery controls</p></div>{error&&<div role="alert" className="rounded-md border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-400">{error}</div>}<Card><div className="border-b border-[#20293a] p-3"><div className="relative max-w-md"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-600"/><input aria-label="Search campaigns" className="crm-input pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search campaigns..."/></div></div>{filtered.length?<div className="crm-responsive-table overflow-x-auto"><table className="w-full"><thead className="crm-table-head"><tr><th className="px-4 py-3">Campaign</th><th className="px-4 py-3">Client</th><th className="px-4 py-3">Spend</th><th className="px-4 py-3">Reach</th><th className="px-4 py-3">Results</th><th className="px-4 py-3">Status</th>{canManage&&<th className="px-4 py-3 text-right">Action</th>}</tr></thead><tbody>{filtered.map(c=><tr key={c._id} className="hover:bg-white/[0.02]"><td className="crm-table-cell"><p className="font-semibold text-slate-200">{c.name}</p><p className="text-xs text-slate-500">{c.objective} · {c.platform}</p></td><td className="crm-table-cell">{c.client?.name||"Unassigned"}</td><td className="crm-table-cell font-semibold text-slate-200">{formatMoney(c.performance.spend,c.budget.currency)}</td><td className="crm-table-cell">{c.performance.reach.toLocaleString()}</td><td className="crm-table-cell"><span className="text-blue-400">{c.performance.results.toLocaleString()}</span><p className="text-xs text-slate-500">{formatMoney(c.performance.costPerResult,c.budget.currency)} CPR</p></td><td className="crm-table-cell"><StatusBadge tone={c.status==="active"?"success":c.status==="paused"?"warning":c.status==="failed"?"danger":"default"}>{c.status}</StatusBadge></td>{canManage&&<td className="crm-table-cell"><div className="flex justify-end"><button disabled={busy===c._id} onClick={()=>update(c,c.status==="active"?"paused":"active")} className="crm-icon-button" aria-label={c.status==="active"?`Pause ${c.name}`:`Start ${c.name}`} title={c.status==="active"?"Pause campaign":"Start campaign"}>{c.status==="active"?<Pause className="size-3.5"/>:<Play className="size-3.5"/>}</button></div></td>}</tr>)}</tbody></table></div>:<div className="crm-empty"><Megaphone className="size-5"/>No campaigns found.</div>}</Card></div> }
