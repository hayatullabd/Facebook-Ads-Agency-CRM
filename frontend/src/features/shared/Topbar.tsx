import { LogOut, Menu } from "lucide-react";

export function Topbar({ title, role, userName, onMenu, onLogout }: { title: string; role: string; userName: string; onMenu: () => void; onLogout: () => void; }) {
  return <header className="sticky top-0 z-30 flex min-h-16 items-center justify-between border-b border-[var(--border)] bg-[#0d1210]/95 px-4 py-2 backdrop-blur-xl sm:px-6"><div className="flex min-w-0 items-center gap-3"><button className="crm-icon-button lg:hidden" onClick={onMenu} aria-label="Open navigation" title="Open navigation"><Menu className="size-4" /></button><div className="min-w-0"><p className="font-mono text-[10px] uppercase tracking-[.14em] text-emerald-300">Operations / {role}</p><h1 className="mt-0.5 truncate text-base font-semibold tracking-[-.02em] text-slate-100 sm:text-lg">{title}</h1></div></div><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-xs text-slate-300">{userName}</p><p className="font-mono text-[9px] uppercase tracking-[.12em] text-emerald-300">Session active</p></div><button onClick={onLogout} className="crm-icon-button" aria-label="Log out" title="Log out"><LogOut className="size-4" /></button></div></header>;
}
